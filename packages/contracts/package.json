{
  "name": "@news/contracts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shared API contract — Zod schemas and the types inferred from them. Imported by both the API and the web client so validation rules cannot drift.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
