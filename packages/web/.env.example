# Only needed when the client and API are served from different origins.
# Left unset, the app calls the relative /api path: Vite proxies it in
# development, and the two share an origin in production.
# VITE_API_BASE_URL=http://localhost:3001/api
